# 부가가치세 전처리 모듈 구현

## 개요
슈도코드의 `[모듈 1] 부가가치세 전처리` 부분을 실제 코드로 구현합니다. 입력된 총매출(공급대가)에서 순수 매출(공급가액)과 부가세를 분리하는 핵심 로직입니다.

## 작업 내용
- `f부가세_전처리_프로세스` 함수 구현
- 공급가액 산출 로직 구현 (총금액 / 1.1)
- 납부할 부가세 산출 로직 구현 (순수 매출액 * 0.1)
- 다양한 입력값(정수, 소수점 등)에 대한 단위 테스트 작성

## 산출물
- 부가가치세 전처리 서비스 클래스
- 관련 단위 테스트 코드
